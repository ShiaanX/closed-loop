# Copy this file to .env and fill in the real values. .env is gitignored —
# never commit real credentials. Used by krishna_metrics.py (via closed_loop.py).

INFLUX_BASE_URL=https://<your-instance>.timestream-influxdb.<region>.on.aws:8086
INFLUX_ORG=cnc-org
INFLUX_BUCKET=cnc-data-v2
INFLUX_USERNAME=admin
INFLUX_PASSWORD=changeme
