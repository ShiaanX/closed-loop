# part data now lives on Google Drive (G:\My Drive\Closed Loop\Clean Data),
# shared/synced across the team — not committed here any more. See README.
data/

# secrets — never commit
.env

# generated report artifacts — regenerate with closed_loop.py, don't track
krishna_report.html
krishna_metrics.json
**/closed_loop_record.json

# python
__pycache__/
*.pyc

# OS / PowerMill lock junk that sometimes rides along in the CAM Project folders
desktop.ini
lockfile
Thumbs.db
